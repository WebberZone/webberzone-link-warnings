(function(){'use strict';let modal=null;let modalTitle=null;let modalMessage=null;let modalUrl=null;let modalContinue=null;let modalCancel=null;let currentLink=null;let focusableElements=[];let firstFocusable=null;let lastFocusable=null;function init(){document.addEventListener('click',handleLinkClick);modal=document.getElementById('wz-bel-modal');if(!modal){return}
modalTitle=modal.querySelector('#wz-bel-modal-title');modalMessage=modal.querySelector('#wz-bel-modal-message');modalUrl=modal.querySelector('.wz-bel-modal-url');modalContinue=modal.querySelector('[data-wz-bel-continue]');modalCancel=modal.querySelector('.wz-bel-modal-cancel');if(typeof wzBelSettings!=='undefined'){modalTitle.textContent=wzBelSettings.modalTitle;modalMessage.textContent=wzBelSettings.modalMessage;modalContinue.textContent=wzBelSettings.continueText;modalCancel.textContent=wzBelSettings.cancelText}
modal.querySelectorAll('[data-wz-bel-close]').forEach(function(element){element.addEventListener('click',closeModal)});modalContinue.addEventListener('click',handleContinue);modal.addEventListener('keydown',handleKeydown)}
function handleLinkClick(e){const link=e.target.closest('a[data-wz-bel-external]');if(!link){return}
const method=typeof wzBelSettings!=='undefined'?wzBelSettings.warningMethod:'inline';if('redirect'===method||'inline_redirect'===method){const redirectUrl=link.getAttribute('data-wz-bel-redirect-url');if(redirectUrl){e.preventDefault();window.location.href=redirectUrl;return}}
if('modal'===method||'inline_modal'===method){e.preventDefault();currentLink=link;showModal(link)}}
function showModal(link){const url=link.getAttribute('data-wz-bel-url');modalUrl.textContent=url;modal.removeAttribute('hidden');document.body.classList.add('wz-bel-modal-active');setupFocusTrap();if(firstFocusable){firstFocusable.focus()}}
function closeModal(){modal.setAttribute('hidden','');document.body.classList.remove('wz-bel-modal-active');if(currentLink){currentLink.focus()}
currentLink=null}
function handleContinue(){if(!currentLink){return}
const url=currentLink.getAttribute('data-wz-bel-url');window.open(url,'_blank','noopener,noreferrer');closeModal()}
function setupFocusTrap(){focusableElements=modal.querySelectorAll('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])');firstFocusable=focusableElements[0];lastFocusable=focusableElements[focusableElements.length-1]}
function handleKeydown(e){if('Escape'===e.key){closeModal();return}
if('Tab'===e.key){if(e.shiftKey){if(document.activeElement===firstFocusable){e.preventDefault();lastFocusable.focus()}}else{if(document.activeElement===lastFocusable){e.preventDefault();firstFocusable.focus()}}}}
if('loading'===document.readyState){document.addEventListener('DOMContentLoaded',init)}else{init()}})()