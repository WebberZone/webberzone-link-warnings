(function($){'use strict';function initTomSelect(){const elements=document.querySelectorAll('.ts_autocomplete');elements.forEach(function(element){const prefix=element.getAttribute('data-wp-prefix')||'WZ';const settingsKey=`$ {
	prefix
}

TomSelectSettings`;const settings=window[settingsKey]||WZTomSelectSettings;const action=element.getAttribute('data-wp-action')||settings.action;const nonce=element.getAttribute('data-wp-nonce')||settings.nonce;const endpoint=element.getAttribute('data-wp-endpoint')||settings.endpoint;const forms=settings.forms;const tags=settings.tags;const custom_fields=settings.custom_fields;const strings=settings.strings;const options=endpoint==='forms'?forms:(endpoint==='tags'?tags:(endpoint==='custom_fields'?custom_fields:[]));if(!options||!Array.isArray(options)){console.error('Invalid options for endpoint:',endpoint);return}
const formattedOptions=options.map(item=>({value:item.id,text:item.name}));const savedIds=element.value.split(',').map(id=>id.trim()).filter(Boolean);if((endpoint==='category'||endpoint==='post_tag'||endpoint.includes('tax'))&&savedIds.length>0){const savedOptions=savedIds.map(savedValue=>{const match=savedValue.match(/^(.*)\s+\(.*:\d+\)$/);const termName=match?match[1]:savedValue;return{value:savedValue,text:termName}});const allOptions=[...formattedOptions];savedOptions.forEach(savedOption=>{if(!allOptions.some(opt=>opt.value===savedOption.value)){allOptions.push(savedOption)}});formattedOptions.length=0;formattedOptions.push(...allOptions)}
if(!(endpoint==='category'||endpoint==='post_tag'||endpoint.includes('tax'))&&savedIds.length>0){savedIds.forEach(savedValue=>{if(!formattedOptions.some(opt=>opt.value===savedValue)){formattedOptions.push({value:savedValue,text:savedValue})}})}
let customConfig={};const configAttr=element.getAttribute('data-ts-config');if(configAttr){try{customConfig=JSON.parse(configAttr)}catch(e){console.error('Error parsing custom config:',configAttr,e)}}
const defaultConfig={plugins:['dropdown_input','clear_button','remove_button'],valueField:'value',labelField:'text',searchField:['text','value'],options:formattedOptions,items:savedIds,persist:!0,createOnBlur:!1,create:!1,render:{no_results:(data,escape)=>`<div class="no-results">$ {
	strings.no_results.replace('%s', escape(data.input))
}

</div>`,option:(data,escape)=>{if(endpoint==='category'||endpoint==='post_tag'||endpoint.includes('tax')){return `<div>$ {
	escape(data.value)
}

</div>`}
if(data.text===data.value){return `<div>$ {
	escape(data.value)
}

</div>`}
return `<div>$ {
	escape(data.text)
}

($ {
	escape(data.value)
}

)</div>`},item:(data,escape)=>{if(endpoint==='category'||endpoint==='post_tag'||endpoint.includes('tax')){return `<div>$ {
	escape(data.value)
}

</div>`}
if(data.text===data.value){return `<div>$ {
	escape(data.value)
}

</div>`}
return `<div>$ {
	escape(data.text)
}

($ {
	escape(data.value)
}

)</div>`}},load:function(query,callback){if(!query.length){callback();return}
$.ajax({url:ajaxurl,type:'POST',dataType:'json',data:{action:action,nonce:nonce,q:query,endpoint:endpoint},error:function(){callback()},success:function(res){if(res.success&&Array.isArray(res.data)){callback(res.data)}else{callback()}}})}};const finalConfig={...defaultConfig,...customConfig};try{new TomSelect(element,finalConfig)}catch(error){console.error('Tom Select initialization error:',error)}})}
$(document).ready(initTomSelect)})(jQuery)