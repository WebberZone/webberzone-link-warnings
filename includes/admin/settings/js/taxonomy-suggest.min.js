jQuery(document).ready(function($){$.fn.WZTagsSuggest=function(options){var cache;var last;var $element=$(this);options=options||{};var taxonomy=options.taxonomy||$element.attr('data-wp-taxonomy')||'category';var tag_search=options.tag_search||$element.attr('data-wp-action')||'wz_tags_search';delete(options.taxonomy);delete(options.tag_search);function split(val){return val.split(/,(?=(?:(?:[^"]*"){2})*[^"]*$)/)}
function extractLast(term){return split(term).pop()}
options=$.extend({minLength:2,position:{my:'left top+2',at:'left bottom',collision:'none'},source:function(request,response){var term;if(last===request.term){response(cache);return}
term=extractLast(request.term);if(last===request.term){response(cache);return}
$.ajax({type:'POST',dataType:'json',url:ajaxurl,data:{action:tag_search,tax:taxonomy,q:term},}).done(function(data){cache=data;response(data)});last=request.term},search:function(){var term=extractLast(this.value);if(term.length<2){return!1}},focus:function(event,ui){event.preventDefault()},select:function(event,ui){var terms=split(this.value);var val=ui.item.value;if(val.indexOf(',')!==-1){val='"'+val+'"'}
terms.pop();terms.push(val);terms.push("");this.value=terms.join(", ");return!1}},options);$element.on("keydown",function(event){if(event.keyCode===$.ui.keyCode.TAB&&$(this).autocomplete('instance').menu.active){event.preventDefault()}}).autocomplete(options)};$('.category_autocomplete').each(function(i,element){$(element).WZTagsSuggest()})})